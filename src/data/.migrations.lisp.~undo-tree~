(undo-tree-save-format-version . 1)
"f31f549cce2f069cff6a17bb9fecbb6abd382e70"
[nil nil nil nil (27237 61789 359687 0) 0 nil]
([nil nil ((#("(in-package :cl)

(defpackage :lumen.data.migrations
  (:use :cl)
  (:import-from :lumen.utils
    :str-prefix-p)
  (:import-from :lumen.data.db
    :with-tx :exec :query-a :query-1a :ensure-connection :run-in-transaction)
  (:export
    :defmigration
    :migrate-all :migrate-up-to :rollback-one :status :reset!))
(in-package :lumen.data.migrations)

;;;; Registry (Isolé par Application)
(defstruct migration app-name version up-forms down-forms)

;; *registry* devient une hash-table. 
;; Clé: Nom de l'app (Keyword), Valeur: Tableau de migrations
(defvar *registry* (make-hash-table :test 'eq))
(defvar *lock* (bt:make-lock \"lumen.migrations\"))

(defun %to-version-string (v)
  \"Normalise VERSION en string. Accepte integer ou string.\"
  (etypecase v
    (integer (write-to-string v :base 10))
    (string  v)))

(defun %app-registry (app-name)
  \"Retourne le tableau des migrations pour une app donnée.\"
  (gethash app-name *registry* (make-array 0 :adjustable t :fill-pointer 0)))

(defun %push-migration! (app-name m)
  (bt:with-lock-held (*lock*)
    (let ((app-reg (%app-registry app-name)))
      ;; On nettoie l'existant si on recharge le fichier (pour éviter les doublons en dev)
      (setf app-reg (delete-if (lambda (x)
                                 (string= (migration-version x)
                                          (migration-version m))) 
                               app-reg))
      (vector-push-extend m app-reg)
      ;; Tri par version
      (let* ((lst (loop for i below (length app-reg) collect (aref app-reg i)))
             (sorted (sort lst (lambda (a b)
                                 (string< (migration-version a)
                                          (migration-version b))))))
        (setf (gethash app-name *registry*)
              (make-array (length sorted)
                          :adjustable t
                          :fill-pointer (length sorted) 
                          :initial-contents sorted))))))

(defun %transform-migration-body (forms)
  (let ((executable-forms
          (loop for f in forms
                collect (if (stringp f)
                            `(lumen.data.db:exec ,f)
                            f))))
    `(cl:progn ,@executable-forms)))

;;; --- ANALYSEUR SYNTAXIQUE INTELLIGENT ---
(eval-when (:compile-toplevel :load-toplevel :execute)
  (defun %analyze-and-transform (form)
    (cond
      ((stringp form) `(lumen.data.db:exec ,form))
      ((null form) '(cl:progn))
      ((and (consp form) (symbolp (car form)) (string-equal (symbol-name (car form)) \"PROGN\")) form)
      ((and (consp form) (or (stringp (car form)) (consp (car form))))
       `(cl:progn
          ,@(loop for step in form collect
                  (if (stringp step) `(lumen.data.db:exec ,step) step))))
      (t form))))

(defmacro defmigration (app-name version &key up down)
  \"Définit une migration rattachée à une application (APP-NAME).\"
  (let ((ver-str (if (integerp version) (write-to-string version) version))
        (up-body   (%analyze-and-transform up))
        (down-body (%analyze-and-transform down)))
    
    ;; On s'assure que app-name est un keyword à la compilation
    `(let ((app-kw (intern (string-upcase (string ',app-name)) \"KEYWORD\")))
       (%push-migration! app-kw
         (make-migration
          :app-name   app-kw
          :version    ,ver-str
          :up-forms   #'(lambda () ,up-body)
          :down-forms #'(lambda () ,down-body))))))

(defun %ensure-up-fn (mig) (migration-up-forms mig))
(defun %ensure-down-fn (mig) (migration-down-forms mig))

;;;; Storage
(defun %ensure-table! ()
  (run-in-transaction
   (lambda ()
      (ignore-errors (postmodern:query \"set local client_min_messages = 'error'\"))
      (exec
      \"CREATE TABLE IF NOT EXISTS schema_migrations (
        version text PRIMARY KEY,
        applied_at timestamptz NOT NULL DEFAULT now()
      )\"))))

(defun %applied-list ()
  (ensure-connection
    (multiple-value-bind (rows _n)
        (query-a \"SELECT version, applied_at FROM schema_migrations ORDER BY version ASC\")
      (declare (ignore _n))
      rows)))

(defun %applied-versions ()
  (mapcar (lambda (r) (cdr (assoc :version r))) (%applied-list)))

(defun %find-mig (app-name vstr)
  (let ((reg (%app-registry app-name)))
    (loop for i from 0 below (length reg)
          for m = (aref reg i)
          when (string= (migration-version m) vstr) do (return m))))

(defun %max-registered-version (app-name)
  (let ((reg (%app-registry app-name)))
    (when (> (length reg) 0)
      (migration-version (aref reg (1- (length reg)))))))

;;;; Commands (Mise à jour : Nécessitent app-name)

(defun migrate-all (app-name &key (verbose t))
  \"Applique toutes les migrations non appliquées pour une application spécifique.\"
  (%ensure-table!)
  (let* ((app-kw (intern (string-upcase (string app-name)) \"KEYWORD\"))
         (target (%max-registered-version app-kw)))
    (if target
        (migrate-up-to app-kw target :verbose verbose)
        (progn
          (when verbose (format t \"~&[migrations:~A] nothing to migrate.~%\" app-kw))
          (values 0 0)))))

(defun migrate-up-to (app-name target &key (verbose t))
  (%ensure-table!)
  (let* ((app-kw (intern (string-upcase (string app-name)) \"KEYWORD\"))
         (tgt (%to-version-string target))
         (applied          (%applied-versions))
         (applied-set      (alexandria:alist-hash-table
                            (mapcar (lambda (v) (cons (string-trim \" \" v) t)) applied)
                            :test #'equal))
         (reg        (%app-registry app-kw))
         (all        (loop for i below (length reg) collect (aref reg i)))
         (all-uniq   (remove-duplicates all :key #'migration-version :test #'equal :from-end t))
         (to-up      (remove-if (lambda (m)
                                  (or (gethash (string-trim \" \" (migration-version m)) applied-set)
                                      (string< tgt (migration-version m))))
                                all-uniq))
         (to-down    (remove-if-not (lambda (m)
                                      (and (gethash (string-trim \" \" (migration-version m)) applied-set)
                                           (string< tgt (migration-version m))))
                                    all-uniq))
         (n-up 0) (n-down 0))
    
    ;; UP
    (dolist (m (sort to-up (lambda (a b) (string< (migration-version a) (migration-version b)))))
      (let ((ver (migration-version m)))
        (when verbose (format t \"~&[migrations:~A] ↑ applying ~A...~%\" app-kw ver))
        (run-in-transaction
         (lambda ()
           (funcall (%ensure-up-fn m))
           (exec \"INSERT INTO schema_migrations(version) VALUES ($1) ON CONFLICT DO NOTHING\" ver)))
        (setf (gethash ver applied-set) t)
        (incf n-up)
        (when verbose (format t \"[migrations:~A] OK ~A~%\" app-kw ver))))
    
    ;; DOWN
    (dolist (m (sort to-down (lambda (a b) (string> (migration-version a) (migration-version b)))))
      (let ((ver (migration-version m)))
        (when verbose (format t \"~&[migrations:~A] ↓ rolling back ~A...~%\" app-kw ver))
        (run-in-transaction
         (lambda ()
           (funcall (%ensure-down-fn m))
           (exec \"DELETE FROM schema_migrations WHERE version=$1\" ver)))
        (remhash ver applied-set)
        (incf n-down)
        (when verbose (format t \"[migrations:~A] ROLLBACK OK ~A~%\" app-kw ver))))
    (values n-up n-down)))

(defun rollback-one (app-name &key (verbose t))
  (%ensure-table!)
  (let* ((app-kw (intern (string-upcase (string app-name)) \"KEYWORD\"))
         (last (query-1a \"SELECT version FROM schema_migrations ORDER BY version DESC LIMIT 1\")))
    (if (null last)
        (progn (when verbose (format t \"~&[migrations:~A] nothing to rollback.~%\" app-kw)) 0)
        (let* ((v (cdr (assoc :version last)))
               (m (%find-mig app-kw v)))
          (unless m (error \"No migration registered for version ~A in app ~A\" v app-kw))
          (when verbose (format t \"~&[migrations:~A] ↓ rolling back ~A...~%\" app-kw v))
          (run-in-transaction
           (lambda ()
             (funcall (%ensure-down-fn m))
             (exec \"DELETE FROM schema_migrations WHERE version=$1\" v)))
          (when verbose (format t \"[migrations:~A] ROLLBACK OK ~A~%\" app-kw v))
          1))))

(defun status (app-name)
  (%ensure-table!)
  (let* ((app-kw (intern (string-upcase (string app-name)) \"KEYWORD\"))
         (applied (%applied-list))
         (applied-index (alexandria:alist-hash-table
                         (mapcar (lambda (r) (cons (cdr (assoc :version r)) r)) applied)
                         :test #'equal))
         (reg (%app-registry app-kw)))
    (loop for i from 0 below (length reg)
          for m = (aref reg i)
          for v = (migration-version m)
          for row = (gethash v applied-index)
          collect
          (list (cons :version v)
                (cons :applied-p (and row t))
                (cons :applied-at (and row (cdr (assoc :applied_at row))))))))

(defun reset! ()
  (%ensure-table!)
  (run-in-transaction
   (lambda () (exec \"TRUNCATE TABLE schema_migrations\"))))
" 0 1 (fontified t) 1 11 (face font-lock-keyword-face fontified t) 11 12 (fontified t) 12 15 (face font-lock-builtin-face fontified t) 15 19 (fontified t) 19 29 (face font-lock-keyword-face fontified t) 29 30 (fontified t) 30 52 (face font-lock-type-face fontified t) 52 56 (fontified t) 56 60 (face font-lock-builtin-face fontified t) 60 61 (fontified t) 61 64 (face font-lock-builtin-face fontified t) 64 69 (fontified t) 69 81 (face font-lock-builtin-face fontified t) 81 82 (fontified t) 82 94 (face font-lock-builtin-face fontified t) 94 99 (fontified t) 99 112 (face font-lock-builtin-face fontified t) 112 117 (fontified t) 117 129 (face font-lock-builtin-face fontified t) 129 130 (fontified t) 130 144 (face font-lock-builtin-face fontified t) 144 149 (fontified t) 149 157 (face font-lock-builtin-face fontified t) 157 158 (fontified t) 158 163 (face font-lock-builtin-face fontified t) 163 164 (fontified t) 164 172 (face font-lock-builtin-face fontified t) 172 173 (fontified t) 173 182 (face font-lock-builtin-face fontified t) 182 183 (fontified t) 183 201 (face font-lock-builtin-face fontified t) 201 202 (fontified t) 202 221 (face font-lock-builtin-face fontified t) 221 226 (fontified t) 226 233 (face font-lock-builtin-face fontified t) 233 238 (fontified t) 238 251 (face font-lock-builtin-face fontified t) 251 256 (fontified t) 256 268 (face font-lock-builtin-face fontified t) 268 269 (fontified t) 269 283 (face font-lock-builtin-face fontified t) 283 284 (fontified t) 284 297 (face font-lock-builtin-face fontified t) 297 298 (fontified t) 298 305 (face font-lock-builtin-face fontified t) 305 306 (fontified t) 306 313 (face font-lock-builtin-face fontified t) 313 317 (fontified t) 317 327 (face font-lock-keyword-face fontified t) 327 328 (fontified t) 328 350 (face font-lock-builtin-face fontified t) 350 353 (fontified t) 353 358 (face font-lock-comment-delimiter-face fontified t) 358 391 (face font-lock-comment-face fontified t) 391 392 (fontified t) 392 401 (face font-lock-keyword-face fontified t) 401 402 (fontified t) 402 411 (face font-lock-type-face fontified t) 411 451 (fontified t) 451 454 (face font-lock-comment-delimiter-face fontified t) 454 490 (face font-lock-comment-face fontified t) 490 493 (face font-lock-comment-delimiter-face fontified t) 493 552 (face font-lock-comment-face fontified t) 552 553 (fontified t) 553 559 (face font-lock-keyword-face fontified t) 559 560 (fontified t) 560 570 (face font-lock-variable-name-face fontified t) 570 588 (fontified t) 588 593 (face font-lock-builtin-face fontified t) 593 601 (fontified t) 601 607 (face font-lock-keyword-face fontified t) 607 608 (fontified t) 608 614 (face font-lock-variable-name-face fontified t) 614 629 (fontified t) 629 647 (face font-lock-string-face fontified t) 647 652 (fontified t) 652 657 (face font-lock-keyword-face fontified t) 657 658 (fontified t) 658 676 (face font-lock-function-name-face fontified t) 676 683 (fontified t) 683 740 (face font-lock-doc-face fontified t) 740 744 (fontified t) 744 753 (face font-lock-keyword-face fontified t) 753 788 (fontified t) 788 793 (face font-lock-builtin-face fontified t) 793 819 (fontified t) 819 824 (face font-lock-keyword-face fontified t) 824 825 (fontified t) 825 838 (face font-lock-function-name-face fontified t) 838 852 (fontified t) 852 909 (face font-lock-doc-face fontified t) 909 955 (fontified t) 955 966 (face font-lock-builtin-face fontified t) 966 969 (fontified t) 969 982 (face font-lock-builtin-face fontified t) 982 990 (fontified t) 990 995 (face font-lock-keyword-face fontified t) 995 996 (fontified t) 996 1012 (face font-lock-function-name-face fontified t) 1012 1029 (fontified t) 1029 1046 (face font-lock-keyword-face fontified t) 1046 1061 (fontified t) 1061 1064 (face font-lock-keyword-face fontified t) 1064 1108 (fontified t) 1108 1111 (face font-lock-comment-delimiter-face fontified t) 1111 1193 (face font-lock-comment-face fontified t) 1193 1225 (fontified t) 1225 1231 (face font-lock-keyword-face fontified t) 1231 1451 (fontified t) 1451 1454 (face font-lock-comment-delimiter-face fontified t) 1454 1470 (face font-lock-comment-face fontified t) 1470 1477 (fontified t) 1477 1481 (face font-lock-keyword-face fontified t) 1481 1489 (fontified t) 1489 1493 (face font-lock-keyword-face fontified t) 1493 1500 (fontified t) 1500 1550 (fontified t) 1550 9155 (fontified nil)) . 1) (undo-tree-id0 . -9155) (undo-tree-id1 . -9155) (undo-tree-id2 . -816) (undo-tree-id3 . -816) (undo-tree-id4 . -816) (undo-tree-id5 . -816) (undo-tree-id6 . -816) (undo-tree-id7 . -816) (undo-tree-id8 . -9155) (undo-tree-id9 . -816) (undo-tree-id10 . -816) (undo-tree-id11 . -9155) (t 27164 21358 0 0)) nil (27237 61789 359684 0) 0 nil])
([nil nil ((nil rear-nonsticky nil 27179 . 27180) (nil fontified nil 1 . 27180) (1 . 27180)) nil (27237 61789 359647 0) 0 nil])
([nil nil ((27180 . 27181)) nil (27237 61789 359641 0) 0 nil])
([nil nil ((#("(defun %composite-primary-key-exists-p ()
  \"Vérifie que la clé primaire porte sur (app_name, version).\"
  (let* ((table-name
           (%migration-table-fqn))

         (row
           (query-1a
            \"SELECT EXISTS (
                 SELECT 1
                   FROM pg_constraint AS constraint_info
                  WHERE constraint_info.conrelid =
                        to_regclass($1)
                    AND constraint_info.contype = 'p'
                    AND (
                        SELECT array_agg(
                                   attribute_info.attname
                                   ORDER BY key_info.ordinality
                               )
                          FROM unnest(
                                   constraint_info.conkey
                               )
                               WITH ORDINALITY
                               AS key_info(attnum, ordinality)
                          JOIN pg_attribute AS attribute_info
                            ON attribute_info.attrelid =
                               constraint_info.conrelid
                           AND attribute_info.attnum =
                               key_info.attnum
                    ) = ARRAY[
                        'app_name',
                        'version'
                    ]::text[]
             ) AS exists_p\"
            table-name))

         (value
           (%row-value
            row
            :exists-p
            :exists_p
            :*exists--p*)))

    (%truthy-p value)))" 0 1 (fontified t) 1 6 (face font-lock-keyword-face fontified t) 6 7 (fontified t) 7 38 (face font-lock-function-name-face fontified t) 38 44 (fontified t) 44 104 (face font-lock-doc-face fontified t) 104 108 (fontified t) 108 112 (face font-lock-keyword-face fontified t) 112 209 (fontified t) 209 1352 (face font-lock-string-face fontified t) 1352 1446 (fontified t) 1446 1455 (face font-lock-builtin-face fontified t) 1455 1468 (fontified t) 1468 1477 (face font-lock-builtin-face fontified t) 1477 1490 (fontified t) 1490 1502 (face font-lock-builtin-face fontified t) 1502 1530 (fontified t)) . -8535) (undo-tree-id0 . -1530) (undo-tree-id1 . -979) (undo-tree-id2 . -7) (undo-tree-id3 . -38) (undo-tree-id4 . -38) (undo-tree-id5 . -38) (undo-tree-id6 . -38) (undo-tree-id7 . -38) (undo-tree-id8 . -38) (undo-tree-id9 . -38) (undo-tree-id10 . -38) (undo-tree-id11 . -38) (undo-tree-id12 . -1530) (undo-tree-id13 . -1530) (undo-tree-id14 . -1507) (undo-tree-id15 . -1530) (undo-tree-id16 . -1530) 10065 (t 27237 61789 0 0)) nil (27238 2240 395798 0) 0 nil])
([nil nil ((nil rear-nonsticky nil 9587 . 9588) (nil fontified nil 8535 . 9588) (8535 . 9588)) nil (27238 2242 989447 0) 0 nil] [nil nil ((nil rear-nonsticky nil 8565 . 8566) (nil fontified nil 8535 . 8566) (8535 . 8566)) ((#("%composite-primary-key-exists-p" 0 30 (fontified nil) 30 31 (rear-nonsticky nil fontified nil)) . 8535) (undo-tree-id17 . -31) (nil rear-nonsticky t 8565 . 8566)) (27238 2240 395716 0) 0 nil])
([nil current ((#("
" 0 1 (fontified t)) . 9588) (t 27238 2242 0 0)) nil (27238 2244 749425 0) 0 nil])
nil
nil
